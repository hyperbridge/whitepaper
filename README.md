# Hyperbridge Whitepapers

## Getting Started

Converting from the Google Doc:

1. Export as HTML
2. Use Pandoc to convert to LaTeX

```bash
pandoc index.html -f html -t latex -s -o ../README.md
```

